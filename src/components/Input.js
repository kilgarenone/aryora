import styled from 'react-emotion';

const Input = styled('input')`
  padding: 0.4em 0.7em;
  font-size: 18px;
  margin: 0;
  height: 42px;
  outline: none;
  box-shadow: none;
`;

export default Input;
