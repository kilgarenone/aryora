import React from 'react';
import { css } from 'react-emotion';

const Header = () => (
  <header>
    <div
      className={css`
        padding: 1rem 1.0875rem;
      `}
    >
      <h4>Aryora</h4>
    </div>
  </header>
);

export default Header;
